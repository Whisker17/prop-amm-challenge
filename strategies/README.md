# strategies/

The strategy registry (`docs/DESIGN.md` §4.2). One directory per candidate:
`NNN-<slug>/{lib.rs,NOTES.md}`, `NNNb-<slug>` for a §2.9 variant. `lib.rs` is the self-contained,
committed submission source — the exact artifact a tag would submit. `NOTES.md` is the durable
provenance/fidelity/frozen-space/numbers record.

Sits outside `crates/` so no candidate is ever mistaken for upstream code.

| Id | Name | Source form | Status | Current numbers |
| --- | --- | --- | --- | --- |
| 000-normalizer | 000 Normalizer (as submission) | inherited (`crates/shared/src/normalizer.rs`, unmodified mechanism) | **baseline, not a ranked candidate** — docs/DESIGN.md §2.8's protocol self-check; no frozen space, no fitting (WHI-1195) | see `results/2026-08-20-l1.md`, `results/2026-08-20-grid.md` |
| 001-cpmm-fee | 001 CPMM Fee | inherited (starter, unmodified mechanism) | fitted (WHI-1194) — the 0-line | `FEE_BPS = 66`; avg edge 399.97, seeds `0..=999`, 10,000 steps, native (train 406.14, validation 401.80 — `NOTES.md` § Fitted point) |
| 003-piecewise-linear | 003 Piecewise Linear | source (Rust) + prose (blog) — `benedictbrady/prop-amm`'s on-chain program | fitted (WHI-1207) — beats the 0-line; committed `DELTA_PCT` range corrected from the porting issue's own `25..=100` to `1..=10` after an exhaustive search showed the original range catastrophic (`NOTES.md` § DELTA_PCT range correction) | `S0_BPS=56, W_BPS=1000, DELTA_PCT=3` — `W_BPS` sits on its own frozen upper bound, `NOTES.md` § Search; avg edge 432.47, seeds `0..=999`, 10,000 steps, native (train 437.56, validation 432.45); 27-cell grid vs. `001`: 22/27 cells favor `003` (`results/2026-08-21-grid-003-piecewise-linear.md`, explained in `NOTES.md` § Grid mode) |
| 004-ewma-shock-decay-fee | 004 EWMA Dynamic Fee + Shock-Decay | source (Rust, direct submission shape) — `lilaclilac09/pamm-a`'s own past competition submission | fitted (WHI-1208) — beats the 0-line and `005`; contains and reproduces the 0-line bit-exact (`NOTES.md` § Containment) | `BASE_BPS=34, VOL_MULT=1, MAX_FEE_BPS=391, SHOCK_FEE_PER_STEP_BPS=0` — shock term ablated to 0 by the search, `NOTES.md` § Search; avg edge 443.75, seeds `0..=999`, 10,000 steps, native (train 451.18, validation 446.30); 27-cell grid vs. `001`: 21/27 cells favor `004` (`results/2026-08-21-grid-004-ewma-shock-decay-fee.md`, explained in `NOTES.md` § Grid mode) |
| 004b-floor-subtracted-ewma-fee | 004b Floor-Subtracted EWMA Fee | §2.9 variant of `004` (from-scratch mechanism change, not an external port) | Pre-registered stop-rule probe (WHI-1223) — the floor-subtraction decoupling thesis is a **negative result**: `max(P1, P2) = 412.70 < 428.8` (the issue's own kill threshold), triggered before any search; closes at `004`'s own containment point — a **bit-exact tie**, not an interior optimum — **no 300-point search run, 0 budget spent** (`NOTES.md` § Negative result) | `BASE_BPS=34, VOL_MULT_NUM=8, FLOOR_BPS=0, MAX_FEE_BPS=391` (bit-exact identical to `004`'s own fitted point) — `NOTES.md` § Containment; avg edge 443.75, seeds `0..=999`, 10,000 steps, native (train 451.18, validation 446.30 — identical to `004`'s own numbers; `bench compare` vs. `004` measures a paired mean diff of exactly `0.000000`, CI `[0.000000, 0.000000]`, n=1,000); 27-cell grid vs. `001`: identical to `004`'s own 21/27 (`results/2026-08-22-grid-004b-floor-subtracted-ewma-fee.md`, explained in `NOTES.md` § Grid mode) |
| 005-vol-adaptive-cpmm-fee | 005 Vol-Adaptive CPMM Fee | source (Rust, direct submission shape) — `dcccrypto/percolator-perp-liquidity`'s `EdgeMax_CumVar.rs` | fitted (WHI-1209) — beats the 0-line | `FEE_LO=5, A_NUM=13, B_DEN=1265` — `FEE_LO` sits on its own frozen lower bound, `NOTES.md` § Search; avg edge 422.93, seeds `0..=999`, 10,000 steps, native (train 430.79, validation 425.95); 27-cell grid vs. `001`: 17/27 cells favor `005` (`results/2026-08-21-grid-005-vol-adaptive-cpmm-fee.md`, explained in `NOTES.md` § Grid mode) |
| 006-hedged-pnl | 006 Hedged PnL (Linear Price Impact) | prose (HackMD) — reduced 2D form per issue review amendment (§2.9's minimum-adaptation set; the doc's literal 4-coefficient formula cannot pass validate for any parameter choice, `NOTES.md` § Fidelity self-assessment) | fitted (WHI-1210) — **negative result**, does not beat the 0-line | `K_SCALED=1639362224` (k≈1.639), `DELTA_BPS=67` — `NOTES.md` § Search; avg edge 369.79, seeds `0..=999`, 10,000 steps, native (train 384.89, validation 379.35, all below `001`'s 399.97/406.14/401.80); 27-cell grid vs. `001`: 18/27 cells favor `006`, but all 9 negative cells are exactly the high-`sigma` cells — a clean, fully-explained fixed-slope-vs-multiplicative-drift split (`results/2026-08-21-grid-006-hedged-pnl.md`, explained in `NOTES.md` § Grid mode) |
| 007-dodo-pmm | 007 DODO PMM (R=ONE, arbitrageur-as-oracle) | source (Solidity library) — `DODOEX/contractV2`'s `PMMPricing.sol`/`DODOMath.sol`, a derivation port (not a transcription) collapsed to `R=ONE` (`NOTES.md` § Provenance) | Step 0.5 probe (WHI-1219) — concentration (`K_BPS<10_000`) is a **negative result**, net-harmful at every tested point; closes at its own `k=1` boundary, a small win over the 0-line — **no 300-point search run, 0 budget spent** (`NOTES.md` § Negative result) | `K_BPS=10_000, FEE_BPS=66` (the CPMM containment point, a boundary hit, not an interior optimum) — `NOTES.md` § Step 0.5; avg edge 401.28, seeds `0..=999`, 10,000 steps, native (train 407.55, validation 403.26, screening 386.05 — all `+1.2` to `+1.5` over `001`'s 399.97/406.14/401.80/384.82); 27-cell grid vs. `001`: 24/27 cells favor `007` with a CI excluding 0, 3 statistical ties, no cell flips sign (`results/2026-08-21-grid-007-dodo-pmm.md`, explained in `NOTES.md` § Grid mode) |
